# image-captioning-cnn-lstm
Image Captioning system using a CNN encoder (ResNet34) and an LSTM decoder with a custom vocabulary built from MS COCO. Includes a binary classifier (ResNet50) to detect Photo vs Others and generate captions only for photographic images
